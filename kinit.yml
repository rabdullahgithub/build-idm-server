---
- name: CONFIGURE AND RESTART IDM
  hosts: all
#  become: yes
  connection: ssh

  tasks:
  - name: KINIT ADMIN
    raw: kinit admin

  - name: PAUSE BEFORE SENDING PASSWORD
    wait_for:
      delay: 5

  - name: SEND PASSWORD
    raw: "{{ my_kerberos_master_password }}"
