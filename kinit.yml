---
- name: CONFIGURE AND RESTART IDM
  hosts: all
#  become: yes
  connection: ssh
  gather_facts: no
  
  tasks:
  - name: KINIT ADMIN
    expect:
      command: kinit admin
      responses:
        (?i)password: "{{ my_kerberos_master_password }}"
